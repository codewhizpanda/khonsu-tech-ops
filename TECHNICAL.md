# Khonsu Tech OPS — Technical Document (Deprecated)

This document described an intermediate architecture — a vanilla ES-module version (`index.html` + `css/` + `js/*.js`) — that has since been fully replaced by a Vue 3 + Vite SPA. It no longer reflects the codebase on `main`.

**For current architecture, see [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md).**

The vanilla ES-module version this document described is preserved at branch `backup/vanilla-js` and tag `v1.0-vanilla-js`, not in the working tree.
